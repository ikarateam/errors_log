# GENERAL ERRORS — DANH SÁCH LỖI CHUNG XUYÊN SUỐT DỰ ÁN (cross-task)

> 🎯 **Mục đích:** Sổ tay lỗi dùng CHUNG cho cả team. Mỗi người / mỗi máy chạy AI (Claude) phải đọc TRƯỚC khi bắt đầu, để **không lặp lại lỗi người khác đã gặp**, giữ cách làm **đồng bộ — nhất quán**, **đỡ mất thời gian chạy lại**.
>
> 🧭 **Cách trình bày (2 tầng — quan trọng):** mỗi lỗi viết theo **2 cách diễn đạt song song**:
> - 🟢 **Nói dễ hiểu (non-tech)** — cho BA / người không rành kỹ thuật / dùng để báo cáo nhanh.
> - 🔧 **Bản kỹ thuật (tech)** — cho dev / AI / người rành, có thuật ngữ + dấu hiệu + cách fix chính xác.
>
> → Nhờ vậy CÙNG một dòng lỗi vừa báo cáo được cho người **không rõ** vừa cho người **rõ**, và log ra **đọc nhanh**.
>
> 🗂️ **Phân loại theo NGUỒN GỐC** (A→E), không chỉ lỗi kỹ thuật: hiểu sai · kỹ thuật · môi trường-đồng bộ · dữ liệu · giao tiếp.
> ✍️ **Cập nhật:** xong mỗi việc → thêm lỗi MỚI chưa có vào đúng nhóm (xem §G). Không trùng.
> 📌 Nguồn ban đầu: `apis/v5.RemoveCommentRecording/BaoCao-Loi-KF28016.md` + phiên migrate v33/34/35/36 (2026-06-09). Mirror: https://github.com/ikarateam/errors_log

---

## A. HIỂU SAI ĐỀ BÀI & MINDSET (hay lặp nhất — cảnh giác cao)

| # | 🟢 Nói dễ hiểu (non-tech) | 🔧 Bản kỹ thuật (tech) | Cách tránh |
|---|---|---|---|
| A1 | Làm sai việc vì hiểu sai yêu cầu ngay từ đầu, phải làm lại từ đầu | Tưởng phải viết skeleton cho API microservice CHƯA tồn tại; thực ra phải test API legacy ĐANG CHẠY rồi mới clone sang MS | Xác nhận RÕ mục tiêu + đầu ra trước khi code; mơ hồ thì hỏi 1 câu chốt, đừng đoán |
| A2 | Test nhầm sang tính năng/bản khác | Nhắm sai servlet / sai version (`/v34` vs `/v35`…), test không phản ánh luồng thật | Đối chiếu đúng tên + version đang chạy; đọc source 2 phía |
| A3 | Làm quá tay, lo xa không cần thiết, đào lan man | Over-engineer; sợ "vỡ fixture" vô căn cứ, probe lan man ngoài scope | Bám đúng phạm vi; nghi "sợ vỡ" → kiểm tra bằng 1 probe, đừng tự suy diễn |
| A4 | Đang làm dở việc này lại nhảy sang việc khác | Mở "mặt trận" API khác khi cái đang làm chưa xong/chưa rà rule | Làm xong + verify 1 việc rồi mới sang việc kế |
| A5 | Làm liền tù tì nhiều bước không dừng hỏi | Bulldoze chuỗi ≥3 bước phụ thuộc data, không dừng confirm (sai NLLB pacing) | Việc ≥3 bước phụ thuộc nhau → làm từng bước, DỪNG chờ confirm |
| A6 | Tự ý làm nhiều hơn cái được giao | Chỉ được duyệt bỏ A nhưng tự bỏ luôn B → vượt phạm vi | Chỉ làm đúng cái đã duyệt; đổi gì khác → hỏi |
| A7 | Vội đổ lỗi cho dev trong khi lỗi là của mình | Kết luận "bug sản phẩm" khi thực ra assert/setup của test sai | Probe trực tiếp kiểm chứng trước khi gán lỗi cho dev |
| A8 | Tự bịa thêm trường hợp test không ai yêu cầu | Thấy 1 dòng quirk trong code → tự đẻ thành TC dù không có risk thật | Quirk → ghi RISK/ghi chú chờ dev xác nhận, KHÔNG thành TC |
| A9 | Tự tạo thứ mới khi chưa được phép rồi phải gỡ | Tự tạo env var mới (`Nllb_legacy_*`) → vi phạm anti-var-bloat | Reuse biến `Nllb_` sẵn (pool owner/member/guest/bot — có cặp deviceId+uid); chỉ tạo khi đã hỏi |
| A10 | Mang bộ test mẫu về dùng nhưng quên rà lại theo chuẩn | Clone reference nhưng giữ `to.have.status`, thiếu `responseTime`, tên không dấu | Clone xong PHẢI audit theo CLAUDE_NLLB (§4.1/§4.2/§8.1) ngay |
| A11 | "Rà toàn bộ xem chỗ nào dính" → tự nới sang khu vực KHÔNG phải việc mình | NLLB nói "rà trong đống GROUP", tôi quét + sửa cả `Micro Service/Paypal` (folder ngoài task) → ĐÚNG collection nhưng SAI sub-scope, NLLB hoảng "sao tôi đụng Paypal". Khác C3 (sai collection) | "Toàn bộ" = toàn bộ **trong phạm vi được giao** (chỉ folder GROUP / chỉ KF của mình), KHÔNG phải cả collection. Liệt kê đúng tập folder trước khi sửa; folder lạ → bỏ qua + báo, đừng tự ý vá |
| A12 | Bỏ sót 1 field optional khi probe → hiểu sai hành vi API → suýt báo bug nhầm | get-users-recommend có field `type` (không @NotNull); tôi probe THIẾU type → non-owner ra 500, suýt label "non-owner bị cấm = bug permission". Thực ra 500 = NPE do thiếu `type` (`type.equalsIgnoreCase(null)`); với type đúng (VIP/ADMIN) hành vi hoàn toàn khác | **Grep SOURCE controller/DTO để biết ĐỦ field (kể cả optional) TRƯỚC khi probe + thiết kế TC**; probe quét các giá trị field optional. Đừng dựng model hành vi từ 1 request thiếu field. (gắn A7 — grep source trước khi gán bug) |
| A13 | Đọc code trên máy mình rồi kết luận chắc nịch "thiếu dòng X / lỗi ở hàm Y", nhưng bản đang chạy trên server lại KHÁC bản mình đọc | **Source checkout local ≠ build đã deploy** (DEV/PROD lệch version). Tôi suýt gán "MS thiếu nhánh owner-self-delete" dựa trên file `PostService.java` local; thực tế message + hành vi runtime không khớp dòng code đó → nếu cite dòng code làm root-cause sẽ sai | KHÔNG cite đúng dòng/nhánh code local làm bằng chứng bug khi chưa chắc bản deploy trùng. Neo kết luận vào **fact quan sát được lúc chạy** (message thật + probe GQL/Datastore), code local chỉ để định hướng. Nghi version drift → nói rõ "theo source local…", đừng khẳng định |
| A14 | Tự dựng luồng test mà app thật không làm vậy (vd "xoá nhiều bài 1 lúc") chỉ vì API cho phép | API nhận `postIds=[nhiều id]` (batch) ⇒ tôi build TC batch-delete; nhưng app chỉ xoá TỪNG bài → test không phản ánh luồng business thật, NLLB bắt bỏ | API CHO PHÉP ≠ app DÙNG. Xác nhận luồng client/business thật trước khi thiết kế; test mô phỏng đúng cách người dùng thao tác, đừng tận dụng khả năng kỹ thuật mà luồng thật không chạy |
| A15 | ĐỌC rule rồi (hook xác nhận) nhưng khi GÕ tên folder/TC mới lại bỏ dấu tiếng Việt → "Nguoi khong phai chu phong" thay vì "Người không phải chủ phòng", khó đọc | Rule §4.6 (tên TC/sub-folder tiếng Việt CÓ DẤU) đã nằm trong context, folder gốc cũng đầy dấu, nhưng lúc SINH string tên *trong code Python/JSON* một thói quen generic "ASCII-safe cho code/JSON" tự bật, ghi đè rule. Lỗi ở **bước generate, không phải bước đọc**: đọc ≠ áp dụng; pre-flight scan chỉ soi range/uid, KHÔNG soi "tên đã có dấu chưa" → cái không có trong checklist thì lọt. Postman/Newman/JSON xử Unicode tốt, ASCII-hoá là vô căn cứ | (1) Khi consolidate/rename: **COPY nguyên phong cách tên folder GỐC** (đầy dấu), đừng viết lại từ đầu theo ý mình (gắn anti-pattern "tự đổi format thay vì theo template"). (2) Thêm vào **pre-flight scan SAU build**: flag tên TC/sub-folder + tên `pm.test` nếu chứa từ tiếng Việt mà THIẾU dấu (vd có "duyet/khong/nguoi/du lieu" trần). 0 cảnh báo mới push. (3) Rule generic kỹ thuật KHÔNG được override rule dự án đã ghi rõ — khi 2 cái xung đột, rule dự án thắng |
| A16 | Giả định "mọi trường hợp bị từ chối thì server trả trạng thái lỗi (FAILED)" → viết assert `status==FAILED` cho cả nhánh mà server thực ra trả OK | Có API phát hiện lỗi bằng cách **so khớp từ khóa trong message** thay vì set status đàng hoàng (vd `like-comment`: `if(message.contains("Không")||contains("tồn tại")||contains("đang chặn nhau")...) → FAILED; else → OK`). Chuỗi "Bài thu này đang ở chế độ riêng tư" KHÔNG nằm trong danh sách từ khóa → lọt thành **status=OK** dù là nhánh TỪ CHỐI. Đoán "rejection ⇒ FAILED" sẽ assert sai. Tương tự `res.blocked=true` được set nhưng `ApiResponse.failed(msg)` KHÔNG mang `data` → `data` null, assert `data.blocked` ném TypeError | PROBE TỪNG NHÁNH lấy `status` + `message` THẬT trước khi viết assert (gắn B5/B6); assert đúng cái server TRẢ, không theo cái "đáng lẽ phải vậy". Nhánh từ-chối-nhưng-status-OK là QUIRK code thật → ghi finding, assert OK + message, KHÔNG ép FAILED |
| A17 | Gặp 1 lần API key trả "không hợp lệ" → vội kết luận "key chết, BLOCKED, chờ user cấp lại" → dừng cả nhiệm vụ; thực ra key vẫn đúng, chỉ là server lỗi tạm thời | Postman API (`api.getpostman.com`) thỉnh thoảng trả **401 `Invalid API Key` TRANSIENT** (outage/rate-limit) dù key ĐÚNG. Tôi 401 cả REST lẫn MCP cùng lúc → suy "key rotated/dead" → gán credential-blocker, bàn giao. User gửi lại CHÍNH key cũ (giống hệt) → thử lại PASS ngay. "Cùng dead ở 2 đường" thực ra là 1 nguồn (cùng backend Postman) đang lỗi, KHÔNG phải bằng chứng key sai | TRƯỚC khi gán "credential dead / BLOCKED": (1) **retry 2-3 lần cách vài giây** — 401 transient tự khỏi; (2) so giá trị key local với prefix đã biết — nếu KHÔNG đổi thì khả năng cao là outage chứ không phải rotate; (3) chỉ kết luận "key sai" khi retry vẫn fail VÀ giá trị key thực sự khác/trống. Đừng để 1 lần 401 chặn cả pipeline (gắn D-flaky: lỗi tạm thời ≠ rào bài toán) |
| A18 | Thấy code gọi `I18nUtils.get("message.x", language)` rồi mở file ngôn ngữ của service KHÁC, copy chuỗi tiếng Việt đó làm "kết quả mong đợi" → assert text mà server thật KHÔNG trả ra | Mỗi microservice deploy RIÊNG: classpath i18n chỉ gồm `<service>/resources/i18n` + module `shared` (KHÔNG kéo bundle app khác dù pom có `<other>-shared` lib). `MessageConfig` (shared) đặt `setUseCodeAsDefaultMessage(true)` → **key thiếu KHÔNG ném lỗi mà trả NGUYÊN chuỗi key** làm message. Cùng key `message.room_not_found` → ra tiếng Việt ở live-room nhưng ra literal `"message.room_not_found"` ở mc-user (key chỉ định nghĩa trong live-room app). Suy message từ source-key/bản service khác = assert sai (verified search-user-in-room 2026-06-17) | Message của endpoint mc-* PHẢI **probe live lấy chuỗi THẬT** trước khi assert (gắn B5/B6/A16). grep key trong `<service-đang-test>/resources/i18n` — thiếu → server trả nguyên key → assert literal key (lock + nhắc dev map). Lưu ý chuỗi hardcode thẳng trong code (vd `"Device not found or invalid."`) KHÔNG qua I18nUtils → luôn nguyên văn, không localize |
| A21 | Thấy DEV "vẫn lỗi" sau khi dev báo fix → kết luận "fix chưa ăn", nhưng thực ra fix **CỐ Ý chỉ chạy PROD** (env-gated) và lọc **set role HẸP hơn** yêu cầu chung — tôi đọc source CŨ + assert sai cả env lẫn tập role | top-users: dev thêm `boolean prod = SharedUtils.isMainServer(); if (prod && UserSharedUtils.isCustomerSupporter(user)) continue;` trong `enrich()`. (1) `isMainServer()=ProjectIds.PROD.equals(getProjectId())` → DEV KHÔNG lọc **theo thiết kế** → DEV "leak" account nội bộ là ĐÚNG, không phải bug. (2) `isCustomerSupporter` = set CỤ THỂ `{CUSTOMER-SUPPORTER,MANAGER,ICOIN-MANAGER,TESTER,TESTER_AUTO,SINGER}` (theo yêu cầu CSKH), KHÁC `isInternalAccount` (role∉{USER,COLLABORATOR}) rộng hơn — ADMIN/BOT/EDITOR KHÔNG bị ẩn. Tôi assert "không role nội bộ ở CẢ 2 env" theo def rộng → false-fail DEV + có thể sai PROD. Gốc: đọc source checkout CŨ (chưa `git pull` nhánh dev) → A13 | Trước khi viết assert exclusion/permission + chọn [dev]/[dev,prod]: (1) **`git pull` đúng nhánh deploy** rồi đọc lại — source cũ = vô giá trị (A13). (2) **Tìm env-gate trong chính hàm**: `isMainServer()/isDevelopmentServer()/isCheapServer()` → nếu filter gated PROD thì TC là **PROD-only** (skip DEV, KHÔNG assert giống 2 env). (3) **Dùng ĐÚNG predicate server gọi** (copy nguyên set role từ hàm thật `isCustomerSupporter`), KHÔNG thay bằng định nghĩa "nội bộ" tổng quát của mình. (4) Nếu yêu cầu NLLB ("mọi role nội bộ") RỘNG hơn cái dev implement (subset CSKH) → **báo gap cho NLLB**, đừng tự ý nới/thu assert. (verified 2026-06-19 top-users TC09) |
| A20 | Đọc 1 nhánh `else` "vơ hết phần còn lại" trong source rồi thiết kế TC **khẳng định hành vi đó là ĐÚNG** — trong khi đó thực ra là BUG thiếu validate, dev đang fix | `TopRankingService.queryScorePage`: `if(type==0)…else if(type==1)…else{MONTHLY}` → mọi `type` ngoài 0/1/2 (3, -1, 99) rơi vào tháng, KHÔNG reject. Tôi build 3 TC assert "type=3 trả cùng tập với tháng" (lock hành vi buggy thành contract). NLLB: "ngoài 0/1/2 phải BÁO LỖI, dev đang fix" → catch-all `else` = lỗ thiếu `@Min/@Max`/validate, không phải spec | Nhánh catch-all (`else`/default vơ input ngoài miền) **nghi là thiếu-validate, KHÔNG mặc nhiên là spec**. Trước khi viết TC cho input ngoài miền: xác nhận INTENT (spec/SDD/hỏi NLLB), đừng assert hành vi hiện tại như đúng. Khi chưa chốt: viết TC kỳ vọng **báo lỗi** (assert mở `code!==200 || status!==OK`) + ghi rõ "đỏ honest tới khi dev fix", KHÔNG lock theo hành vi quan sát được. (gắn A8 quirk→ghi risk; A16 giả định status) |
| A21 | Đặt tên request trong TC bằng **tag tự chế** (`[Danh sách]`/`[Thả tim]`/`[Chuẩn bị]`/`[Đối chiếu GAE]`) thay vì bộ tên CHỐT → NLLB bắt "thiếu [Before]/[Action]/[Recheck]/[Cleanup]" | Build folder mc-recording/likes: tôi tự đặt tên request theo nghiệp vụ cho "dễ đọc" → vi phạm **CLAUDE_NLLB §5 (HARD RULE NLLB chốt 2026-06-18)**: request CHỈ được mang 1 trong 5 trạng thái `[Setup]/[Before]/[Action]/[Recheck]/[Cleanup]`, KHÔNG tự chế. Gốc lỗi: context phiên đang cache **bản §5 CŨ** (chỉ có bảng BARC, chưa có 5-state) → tôi không mở lại file đọc bản MỚI trước khi build → "không đọc đủ file" (gắn A10 clone-xong-phải-audit). Hệ quả Newman vẫn XANH (tên không ảnh hưởng assert) nên không tự lộ — chỉ NLLB review mắt mới bắt | TRƯỚC khi build artifact: **MỞ LẠI `CLAUDE_NLLB.md §5` + `step3` đọc bản hiện tại trên đĩa** (đừng tin bản trong context có thể cũ — file rule NLLB sửa thường xuyên, mỗi lần "chốt 2026-..." là 1 bản mới). Request name = ĐÚNG bộ 5 tag; read-only→chỉ `[Action]` (+`[Recheck]` nếu đối chiếu DB); B-A-R→`[Before]→[Action]→[Recheck]→[Cleanup]`. Mỗi TC sub-folder kèm description 4 mục (Test gì/Bối cảnh/Hành động/Kết quả). Pre-flight scan thêm: tên request có nằm trong {Setup,Before,Action,Recheck,Cleanup} không. |
| A22 | Pool fixture toàn role TESTER làm API "không chạy nghiệp vụ" → suýt kết luận "happy path BLOCKED, chỉ test được nhánh lỗi" | API ghi-nhận-hành-vi (stop-listening-timer / increase-view / log) có gate `isCustomerSupporter(user)` → role ∈ {CUSTOMER-SUPPORTER,MANAGER,ICOIN-MANAGER,**TESTER**,TESTER_AUTO,SINGER} → trả OK **no-op** (không để lại dấu vết). Pool NLLB TOÀN TESTER → cả START lẫn STOP bị short-circuit → fixture TESTER KHÔNG chạm happy path. Đây là **rào-lựa-chọn** (do role fixture), KHÔNG phải rào-bài-toán | Probe role pool TRƯỚC (GqlQuery `User.role`). Bị short-circuit → **hạ tạm viewer TESTER→USER** qua `POST {{base_url}}/rest/privilegeAdmin/create {uid,role:"USER"}` (chạy dev+prod, no token), chạy luồng thật, **khôi phục→TESTER** ở `[Cleanup]` + verify role==TESTER sau cùng (đừng để USER residue). KHÔNG vội [dev]/BLOCKED. Nhánh no-trace dùng account RIÊNG (owner giữ TESTER) tránh xung đột. ⚠ Endpoint write nặng (STOP reindex ES đồng bộ) → responseTime PROD dao động 0.5–13s → BỎ assert responseTime<1500 (gắn §4.4 ngoại lệ), đừng lock. (verified stop-listening-timer 2026-06-18, memory `feedback_role_lower_for_no_trace_api`) |
| A23 | Probe API feed/recommend **cá nhân hoá** bằng device CHƯA đăng nhập → thấy rỗng → kết luận "ES/DEV env này không có data" (thậm chí lỡ ghi vào test-cases nháp) → suýt thiết kế cả bộ TC theo "DEV chỉ verify hợp đồng rỗng" | Feed cá nhân hoá (mc-recording/v3/newsfeed) query ES có mệnh đề match/boost theo **followee của user đăng nhập**; un-logged → `userShared=null` → `userId=deviceId` → KHÔNG followee → chỉ còn fallback `language.keyword==language`, env không có bài đúng-language-trong-window → **rỗng GIẢ**. Đăng nhập user thật (có followee) → feed ĐẦY (DEV thực ra ~15-19 bài, KHÔNG rỗng). Gốc: kết luận môi trường từ probe THIẾU điều kiện kích hoạt nội dung (giống A12 thiếu field optional) | Với feed/recommend cá nhân hoá: **probe SAU [Setup] login đúng slot** (`en.yokara`→linkedUserYokara, `vi`→linkedUser) rồi mới kết luận env có/không data. Đọc query ES xem nội dung phụ thuộc gì (followee/vipLevel/online) → dựng đủ điều kiện đó trước khi nói "env rỗng". Newman có [Setup] login → tin số liệu Newman hơn probe tay anonymous. (verified newsfeed 2026-06-19: anonymous→0, owner-logged→~19, DEV+PROD 33/33 ×3) |
| A24 | Giả định "API login nào userId/token lạ cũng trả 'không tồn tại'" → viết TC user-not-found cho MỌI method → có method TỰ TẠO account khi lạ → assert đỏ + (tệ hơn) **tạo data rác** | Auto-create KHÁC NHAU theo từng login service dù chung `LoginService.login()`: `login/apple` (LoginAppleService) `if(user==null) newUserApp(...)` → userId lạ = **đăng ký mới** (status OK, KHÔNG "user not found"). `login/recent≡related` (LoginRelatedService) chỉ `findById` → null = "Người dùng không tồn tại". Tôi clone TC user-not-found sang apple → apple trả OK (đỏ) + tạo user ghost trong datastore. Verified mc-user login 2026-06-23 | TRƯỚC khi viết bất kỳ negative "không tồn tại / sai input": **đọc TỪNG service xử method đó** (đừng suy từ 1 method sang cả nhóm). Method có nhánh `newUser*()/create` khi null → KHÔNG có ca user-not-found, bỏ TC đó. Mỗi method = 1 luồng riêng kể cả khi share entrypoint (gắn A12/A16 probe từng nhánh) |
| A19 | Build CẢ bộ TC mới rồi tuyên bố "done + verified" (Newman xanh 3-run) NHƯNG chưa từng mở đọc file rule viết test (`CLAUDE_NLLB.md`) → artifact sai chuẩn ở những rule mới nhất, NLLB nhìn cấu trúc là biết ngay "chưa nạp đủ rule" | Hook SessionStart `cat` cả `CLAUDE_NLLB.md`+`general-errors.md` vào context nhưng tôi chỉ thấy **preview 2KB** (output bị truncate, lưu ra file) → tưởng đã có rule, thực ra chưa đọc thân. Bỏ luôn bước re-read bắt buộc của NLLB-GUARD ("TRƯỚC khi chốt thiết kế TC: đọc lại 3 file rule"). Hậu quả: vi phạm §5 (request trong TC phải tên `[Setup]/[Before]/[Action]/[Recheck]/[Cleanup]` — chốt 2026-06-18, MỚI), §4.4 (bỏ hẳn responseTime thay vì gate ngưỡng cao cho call AI), §4.1 (truyền arg-2 cho `pm.expect`). Newman vẫn XANH vì các lỗi này là sai-CHUẨN, không phải sai-assert → "xanh ≠ đúng chuẩn" | TRƯỚC khi build artifact: **MỞ ĐỌC THẬT** `CLAUDE_NLLB.md` (§4 + §5) + `step2`/`step3` + `general-errors.md` bằng Read, KHÔNG tin preview hook (hook truncate → đọc file persisted nếu cần). Hook cat ≠ đã-đọc. SAU build, chạy **audit checklist** đối chiếu §4.1/§4.2/§4.3/§4.4/§4.6/§4.7 + §5 (mọi request có prefix 5-trạng-thái chưa? responseTime trên [Action] chính? pm.expect không arg-2?) → rồi mới sign-off "done". Rule mới-chốt (xem ngày) ưu tiên kiểm trước. **⭐ Audit THỦ CÔNG vẫn lọt (đã tái phạm: build KF-28049 không đánh số TC + không tag BARC) → 2026-06-18 thêm GATE MÁY: `docs_migrate/postman/lint_tc_structure.py` (kiểm cơ học R0 tag tester / R1 không request phẳng / R2 tên `TC<NN> - …` / R3 request có 1 trong 5 tag / R4 ≤80 / R5 no-range-status / R6 responseTime chỉ ở [Action]) + PreToolUse hook `lint_before_newman.sh` CHẶN `newman run` (exit 2) khi folder NLLB sai chuẩn. → KHÔNG claim "Newman xanh" trên artifact sai cấu trúc. Lời hứa "đã đọc rule" KHÔNG enforce được — gate máy mới ép** |

---

## B. KỸ THUẬT DỰNG & CHẠY TEST (Postman / Newman / legacy GAE)

| # | 🟢 Nói dễ hiểu (non-tech) | 🔧 Bản kỹ thuật (tech) — dấu hiệu & fix | Cách tránh |
|---|---|---|---|
| B1 | Quên "đóng dấu/ký" yêu cầu nên server từ chối | Mất bước ký: body phải là `parameters=<base64(json)>-<key>` (ký ở pre-request cấp collection/folder). Dấu hiệu: `{"status":"FAILED","message":"ERROR"}` / 322B thiếu field. Cần collection var `password=7123358024`, `password2=3564` | Giữ script ký; request tự ký per-request thì append ký vào CUỐI pre-request (sau khi set var) |
| B2 | Địa chỉ máy chủ ghép sai nên đường dẫn hỏng | `host:["{{base_url}}"]` với base_url đã chứa `https://` → newman dựng URL hỏng, trả `<html><head>` → JSONError | Để url dạng string `"{{base_url}}/path"` + truyền base_url qua `--environment`/`--env-var` |
| B3 | Bắn dồn quá nhanh nên bị chặn tốc độ | GAE throttle → trả HTML thay JSON, JSONError "Unexpected token '<'" hàng loạt | Newman `--delay-request 1200` trở lên |
| B4 | Phản hồi có "đuôi rác" bám theo dữ liệu | Response legacy có hậu tố `OriginalRequest=...` → `pm.response.json()` ném lỗi parse | Strip trước parse: `JSON.parse(pm.response.text().replace(/OriginalRequest=[\s\S]*$/,''))` |
| B5 | Trường hợp lỗi thiếu mất vài mục dữ liệu | Nhánh error trả `{status,message}` không có `users` → `j.users.length` ném TypeError | Assert null-safe: `pm.expect(!j.users || j.users.length===0).to.be.true` |
| B6 | Mỗi API trả cấu trúc khác nhau, không giống như tưởng | vd `v33.TopUsers` KHÔNG có `status/message`, chỉ `{users,cursor}` → assert `status` fail | Probe response THẬT trước, không giả định shape |
| B7 | Vài thông báo luôn tiếng Việt dù đặt ngôn ngữ khác | i18n: 1 số message hardcode VI bất kể `language` → assert chuỗi `en` fail | Lấy chuỗi message THẬT từ response, không tự dịch |
| B8 | Đổi tên biến chỗ này quên chỗ kia | Chỉ sửa `{{var}}` trong body, quên `pm.environment.get("var")` trong script → lệch | Swap biến phải thay CẢ 3 dạng: `{{var}}`, `"var"`, `'var'` |
| B9 | Môi trường thiếu một thông tin cấu hình quan trọng | Env thiếu `utility_admin_token` (rỗng) → admin/utility call 401 "Thiếu/sai header" | Kiểm env đích đủ biến hạ tầng trước khi chạy; token dùng chung → để collection variable |
| B10 | Điều kiện/quyền đọc từ chỗ khác nơi mình tưởng | VIP gate của `GetVisitHistory` đọc `UserVipScore`+`recountTime`=tháng hiện tại, KHÔNG phải `User.vipLevel` | Đọc SOURCE để biết gate đọc field/entity nào, set đúng nguồn |
| B11 | Lệnh "sửa" không tự tạo dữ liệu; lệnh "tạo" thì lỗi | `entity/edit` → 404 nếu chưa tồn tại; `entity/create/add/upsert` → 500 | Chỉ edit được entity ĐÃ tồn tại; cần fixture có sẵn (BannedObject/UserVipScore…) hoặc seed bằng API chuyên dụng |
| B12 | Ghi xong đọc lại chưa thấy ngay (hệ thống xử lý chậm) | Ban/VIP đọc qua memcache + ghi async taskqueue → read-after-write chưa phản ánh; chập chờn | Busy-wait đủ lâu (ban ~6s; visit-history taskqueue ~15s). Đây là rào BÀI TOÁN, không phải lỗi test |
| B13 | Tên tiêu đề viết hoa/thường nhạy cảm tuỳ máy chủ | Header case-sensitive (vd `X-Admin-Token`) → 401 dù có token | Dùng đúng y tên header như reference; 401 thì kiểm tên + giá trị token |
| B14 | Chạy lẻ một phần, cấu hình chung không tự nạp | `--folder` từ cloud collection: collection-level var đôi khi không resolve → base_url thành literal `{{base_url}}` | Truyền biến qua `--env-var`/`--environment` thay vì dựa collection variable |
| B15 | Dữ liệu test cần độc nhất mỗi lần chạy + dọn sạch | recordingIDLocal trùng giây → cùng record, đếm sai; để rác | localId unique theo mili-giây `new Date().getTime()` + teardown xóa data đã tạo |
| B16 | Sửa 1 folder nhưng lưu đè cả collection → dễ giẫm việc người khác | Patch bằng `putCollection` (ghi đè NGUYÊN collection) cho 1 thay đổi nhỏ: re-save MỌI folder (Paypal/AppApi/KF khác) theo bản đã fetch → ai sửa tay xen giữa fetch↔PUT bị nuốt (lost-update), và dễ vô tình chạm folder ngoài scope | PUT theo **request/folderId** (`PUT collections/{CID}/requests/{RID}`, bỏ field server-managed) — chỉ ghi đúng item cần. Nếu buộc PUT cả collection: refetch ngay sát lúc PUT + chỉ đổi item trong folder của mình. ⭐ **Collection SHARED nhiều tester đang churn (nhiều người PUT-cả-collection ĐỒNG THỜI):** PUT của mày có thể bị PUT của tester khác (dựa GET cũ CHƯA có folder mày) **ĐÈ NGAY** vài giây sau → folder mày biến mất dù API trả `{collection:{id}}` OK. Dấu hiệu: count top-level item nhảy, xuất hiện folder lạ, folder mày vắng khi fetch lại. Mitigation: **GET→lọc-trùng-tên→append→PUT→GET-verify trong VÒNG LẶP, retry tới khi folder mày PERSIST** (verify tên có mặt); rồi chạy **Newman trên FILE collection đã fetch (local path)** thay vì URL cloud → né bị wipe giữa run. (verified mc-user login 2026-06-19) |
| B17 | Test message tiếng Việt PASS ở DEV nhưng FAIL ở PROD (đúng chữ vẫn rớt) | Server resolve user qua `request.language` → link tương ứng (`linkedUserYokara` cho en.yokara, `linkedUser` generic cho vi). Account chỉ login `en.yokara` → request `language=vi` không tìm thấy user → trả nhầm "hết phiên" thay vì message cần test. DEV pass vì account đã từng login vi (có link); PROD chưa | Khi test message `vi` (request language=vi): [Setup] PHẢI login account đó với CẢ `en.yokara` lẫn `vi` (`login_setup(roles,"vi")`). Đừng giả định 1 lần login en đủ cho mọi ngôn ngữ |
| B18 | API nặng (ES/Redis) flaky responseTime ở lần gọi đầu (cold-start) | Lần call đầu trong run = cold (JIT + cache lạnh + ES query) → responseTime spike vượt ngưỡng, dù warm chỉ ~500ms; rớt giả không phải server chậm thật | Thêm **warm-up call** trong [Setup] (gọi chính endpoint 1 lần, chỉ assert HTTP 200) để TC đo lúc đã nóng; hoặc nâng ngưỡng có lý do ghi rõ. KHÔNG để cold-start làm test flaky |
| B19 | Để file tạm chạy Newman trong `/tmp` rồi giữa chừng nó biến mất → Newman báo không thấy file | macOS/sandbox có thể **dọn `/tmp` giữa phiên** → `newman run /tmp/coll_run.json` ENOENT; chạy newman với path tương đối ở sai cwd cũng ENOENT | Để file làm việc Newman (coll/env/output) ở **thư mục ổn định dưới home** (vd `~/.nm_tmp`), KHÔNG dùng `/tmp`; dùng path tuyệt đối hoặc `cd` vào đúng thư mục trước khi chạy. (vẫn giữ rule C4: refetch + xoá cache trước mỗi run) |
| B20 | Test "chờ X giây cho dữ liệu cộng async rồi đọc lại" nhưng thực ra KHÔNG chờ → đọc sớm → fail chập chờn (hay rớt trên CI/PROD) | Pre-request/test dùng **top-level `await`** (vd `const sleep=ms=>new Promise(r=>setTimeout(r,ms)); await sleep(3000);`). **Newman 6.2.2 / Node 16 (Jenkins) KHÔNG hỗ trợ top-level await** → ném `SyntaxError in prerequest-script`, cả block bị bỏ → delay 3s không chạy. Postman app (mới hơn) có thể nuốt được nên "chạy tay ở app thì xanh, lên Jenkins thì đỏ". Dấu hiệu: log Newman có dòng `N⠄ SyntaxError in prerequest-script` ngay trên request vẫn `200 OK` | KHÔNG dùng top-level await trong script Postman. Chờ async = **poll-based** `pm.sendRequest`+`setTimeout` đệ quy (early-exit khi đạt expected, MAX cap) — xem [[feedback_poll_async_score]], [[feedback_retry_pattern_prod]]. Quét script: bất kỳ `await ` ở scope ngoài cùng = cấm. Đừng tin "app chạy xanh" — verify trên ĐÚNG newman/node của CI |
| B21 | Dùng biến env `language` cho API microservice mới → bị 400 "Language must be 'vi' or 'en.yokara'" dù biến có giá trị | Env DEV/PROD có `language = vi.ikara` (chuẩn cho legacy GAE), NHƯNG envelope microservice `ApiRequest.language` validate regex `vi\|en\.yokara\|tl\.okara\|mz\.okara\|lo\.okara` → `vi.ikara` KHÔNG khớp → HTTP 400 `VALIDATION_FAILED`. `{{platform}}` (=ANDROID) thì OK, chỉ `language` lệch | Trong body call microservice mobile (`/mc-user/...`) hardcode `"language":"vi"` (hoặc `en.yokara`), **KHÔNG** dùng `{{language}}`. Biến `{{language}}` chỉ đúng cho servlet legacy. (Envelope ApiRequest: deviceId/platform/language/packageName/version/requestTime đều @NotEmpty/@NotNull — thiếu/sai → 400 VALIDATION_FAILED + `errors.<field>`.) |
| B22 | Test assert response "luôn có trường data" → fail khi data rỗng, dù API đúng | `ApiResponse` (microservice) gắn `@JsonInclude(NON_NULL)` → khi `ApiResponse.ok(null)` (vd bank-account-info không có tài khoản phù hợp level) thì trường `data` **bị bỏ HẲN** khỏi JSON, response chỉ còn `{status:'OK', message:'OK'}`. `pm.expect(res).to.have.property('data')` → AssertionError | Field nullable của response microservice: assert kiểu `const d = res.data; if (d) { ...validate... }` (giống `a_no_field`/legacy-quirks omit-null), KHÔNG assert `to.have.property('data')`. Nhánh rỗng = `status OK` + KHÔNG có `data` là HỢP LỆ |
| B21 | Gắn "đo tốc độ < 1.5s" vào MỌI testcase → 1 lần mạng PROD giật là đỏ cả lượt, dù API đúng | `pm.expect(responseTime).to.be.below(1500)` đặt ở từng TC: PROD thỉnh thoảng spike 2-2.5s lẻ tẻ (GC/network) ở 1 request bất kỳ → nhân rủi ro flaky (mỗi TC = 1 cơ hội đỏ). Rule là **API CHÍNH mỗi TASK** (1 chỗ happy), KHÔNG phải mỗi TC | Chỉ gắn perf-assert ở TC happy chính + thêm warm-up [Setup] (gọi endpoint 1 lần) để đo lúc nóng (gắn B18). TC biến thể (lọc/empty/negative) chỉ assert status+body. Burn recent-live-rooms 2026-06-15: TC02 PROD 2.4s đỏ 1/3 run, gỡ perf khỏi TC02/03 → 30/30 ×3 |
| B22 | Gọi API microservice `/mc-recording/...`, `/mc-user/...` mà ký kiểu legacy (AES `parameters=`) hoặc để `language="vi.ikara"` → 400/VALIDATION_FAILED, tưởng API hỏng | Request microservice là **JSON THUẦN** `{deviceId,language,personalLanguage,packageName,version,requestTime,platform,data:{...}}` + header **`X-Signature: JsjOWX6vnR25qhTpPTFrBwRlrla3QA0g`** (hardcode), KHÔNG base64/AES như legacy. Pre-request collection tự nhận diện qua path (`mobile`/`utility`...) → chỉ upsert header X-Signature, KHÔNG động body. `language` phải ∈ `{vi, en.yokara, lo.okara, mz.okara, tl.okara}` — truyền `vi.ikara` (giá trị env mặc định cho legacy) → `VALIDATION_FAILED "Language must be 'vi' or..."`. `recordingId` nhận cả số (`5152075172872192`) lẫn URL-safe key (`ag1z...`) | Body microservice để nguyên `{{var}}` (Postman tự resolve sau pre-request, KHÔNG bị pre-encode như legacy → set env trong pre-request CÙNG request vẫn kịp). Dùng `language="vi"` cho message VI. Verify state GAE vẫn qua `{{base_url}}/v40.GqlQuery` (pre-request tự ký vì path không phải `mobile`). Clear comment qua Firebase Function `v5-RemoveCommentRecording` (pre-request tự lo idToken + body base64) — chạy cả 2 env |
| B24 | Verify ghi Firebase chỉ qua bản mirror GAE, KHÔNG đọc node RTDB thật (chỗ app hiển thị); HOẶC đọc RTDB bị 401 rồi vội kết luận "RTDB chặn REST" | Comment/like ghi vào **node Firebase RTDB** (`recordingComments/{rid}/{commentId}` + field `likeCount`) — đây là storage CHÍNH client đọc; GAE `RecordingComment` chỉ là backup mirror (có thể lệch nếu transaction Firebase fail mà mirror vẫn tăng). Đọc REST `{{firebase_rtdb_url}}/recordingComments/...json` → **401 Permission denied** → tưởng rules chặn hẳn. THỰC RA thiếu prefix **`/ikara/`**: code dùng `FirebaseUtils.getIkaraRef()` = root `/ikara`, nên path REST phải là `{{firebase_rtdb_url}}/ikara/recordingComments/{recNumber}/{commentId}.json` → **đọc được, KHÔNG cần auth** (public read node đó). idToken-auth KHÔNG cứu (rules chặn theo path) | Verify cả 2 tầng: node Firebase RTDB (GET `…/ikara/recordingComments/{recNum}/{cid}.json` → `message`/`userId`/`likeCount`; reply = `…/{pid}/childs/{cid}.json` có `parentCommentId`) + mirror GAE GqlQuery. Recipe đọc Firebase có sẵn trong collection (folder KF-14856/14857 dùng `…/ikara/recordingComments.json`). Async `setValueAsync`/`runTransaction` → poll self-loop chờ đồng bộ. recNumber = số (vd `Nllb_27857_recording`), KHÔNG phải URL-safe key |
| B23 | Assert cứng HTTP 200 cho TC truyền thiếu/sai field → đỏ vì server trả 400 | 2 LOẠI lỗi, MÃ HTTP KHÁC nhau: (a) **Bean validation `@Valid`/`@NotBlank`/`@Pattern`/`@Min`** (thiếu recordingId, action sai enum, likeNum<1, nội dung rỗng/>1000) → **HTTP 400 + status `VALIDATION_FAILED`**; (b) **lỗi nghiệp vụ** trả qua `ApiResponse.failed(msg)` (commentId trùng, comment không tồn tại, bị chặn, hạn chế bình luận) → **HTTP 200 + status `FAILED`**. Gộp 2 loại vào 1 kiểu assert → 1 nửa đỏ | Phân loại nhánh khi derive: validation-field → `eql(400)`+`VALIDATION_FAILED`; business-reject → `eql(200)`+`FAILED`. Probe live từng nhánh xác nhận mã (gắn B5, no-range). Message validation lấy từ annotation (`message=...` trong payload class), message business lấy từ i18n `messages_vi.properties` hoặc hardcode trong service |
| B25 | Viết TC "thiếu deviceId / platform sai → VALIDATION_FAILED 400" cho endpoint microservice → đỏ vì server trả **200 OK** (expect sai, pass-giả ngược) | Bean-validation envelope (`@NotEmpty/@Pattern` trong `ApiRequest`) CHỈ chạy nếu controller method có **`@Valid` trên `@RequestBody`** — KHÔNG phải endpoint nào cũng có. `RoomEventController.getListRoomEvent` thiếu `@Valid` (0 occ) → gửi thiếu deviceId / platform="WINDOWS" vẫn `200 OK` (verified DEV+PROD 2026-06-16). Ngược lại `LiveRoomController` có 13 `@Valid` → validate chạy. Tức B23 chỉ áp khi method THỰC SỰ có `@Valid` | TRƯỚC khi derive TC envelope-validation: `grep -n "@Valid" <Controller>.java` ngay tại method đang test. Không có `@Valid` → BỎ hẳn TC validation envelope (không tồn tại nhánh đó); negative chỉ còn từ logic THÂN hàm (vd `data` null → NPE 500 INTERNAL_ERROR; cursor rác → `Cursor.fromUrlSafe` ném → 400 BAD_REQUEST). Luôn probe live 1 lần để xác nhận thay vì suy từ annotation trên payload class |
| B26 | Push folder lên Postman bằng `lr_common.folder(name, items)` → folder CHÍNH lên cloud **thiếu hẳn `description`** ⇒ mất tag `#tester:ngocllb` (HARD RULE §4.6) dù build script CÓ biến `DESC`. Tệ hơn: re-push lần 2 **GHI ĐÈ mất tag mà NLLB đã thêm tay** | `lr_common.folder()` (và `_item`) chỉ trả `{"name", "item"}` — KHÔNG có field `description`. Biến `DESC` viết trong build script KHÔNG tự được đính; `push_folder`/push-root chỉ `append(folder(...))` → description = None. Mỗi lần PUT collection lại ghi đè toàn bộ folder → mọi sửa tay (tag, mô tả) bị xoá (gắn feedback_scoped_patch — clobber). Verified 2026-06-16 room-event/list: 2 lần push đều None, NLLB thêm tay bị push thứ 2 wipe | Khi push qua helper: build dict folder chính RỒI **gắn tay** `main = L.folder(FOLDER, items); main["description"] = DESC` trước khi append. DESC PHẢI bắt đầu bằng `#tester:ngocllb`. SAU push, verify lại trên cloud: `'#tester:ngocllb' in folder['description']` (đừng tin biến DESC trong code — verify cái THẬT trên cloud). Pre-flight checklist thêm: "folder chính có description chứa tag chưa?" |
| B27 | Rebuild folder bằng `DELETE /collections/{cid}/folders/{fid}` để xoá bản cũ → endpoint trả **404/000 (im lặng), folder KHÔNG bị xoá** → mỗi lần build TÍCH TỤ folder TRÙNG TÊN; Newman `--folder "X"` match folder ĐẦU (bản cũ stale) → chạy nhầm code cũ, tưởng fix không ăn | Postman API folder-delete (`DELETE .../folders/{id}`) không tin được (404 dù id đúng; sai cid-prefix → 404; URL hỏng → 000). Build script "tự dọn rồi tạo" nếu dựa endpoint này sẽ tạo bản MỚI mà KHÔNG xoá bản cũ → 2,3 folder cùng tên. ⚠ Kèm zsh: `for x in $IDS` KHÔNG word-split (cần `${=IDS}` hoặc array) → URL ghép cả chuỗi → curl 000. Verified 2026-06-16 send-gift: 2 folder trùng, Newman chạy folder cũ → TC04/TC12 "fix rồi vẫn đỏ" | Xoá folder = **full-collection PUT**: `col=GET; col['item']=[it for it in col['item'] if it['name']!=NAME]; PUT {'collection':col}` (giữ nguyên mọi folder khác) → rồi mới tạo lại. Build script đặt bước này ở ĐẦU (idempotent). SAU build: verify `sum(1 for it in col['item'] if it['name']==NAME)==1` (đúng 1 folder, không trùng). Đừng tin folder-delete endpoint |
| B28 | Test-script Newman đỏ `SyntaxError: Identifier 'data' has already been declared` → cả TC chạy KHÔNG được (test-script fail, assertion không đếm) dù logic đúng | Collection `AutomationTest - Ngocllb` có **script cấp-collection/pre-request khai báo sẵn biến `data`** (cùng tên hay dùng cho body). Newman gộp script cấp-collection + cấp-request vào CÙNG scope → request test-script khai báo `const data = r.data` đụng `data` đã có → `const` redeclare ném SyntaxError, nhánh chết. Verified 2026-06-16 user-liveliness-score/list: 4 request có `const data` đều chết, đổi tên xong 53/53 | Trong test-script ĐỪNG khai báo top-level `const/let data` (và các tên dễ đụng: `response`, `request`, `environment`). Dùng tên riêng (`const rd = r.data`). Pre-flight scan build script: `grep "const data\b\|let data\b"` trong các hàm sinh test-script |
| B29 | TC verify "từng phần tử list" bằng chuỗi `pm.sendRequest` (loop N call GqlQuery/Firebase) → giữa run 1 call timeout làm **đứt chuỗi async → assert CUỐI không chạy** → Newman vẫn "0 failed" nhưng SỐ assertion ÍT HƠN run khác = **false-green** (assert kiểm-chứng bị bỏ âm thầm, không đỏ) | `HELPERS.gql` gọi `res.json().gqlQueryEntities` **trần** — khi sendRequest err/timeout thì `res` null → `res.json()` ném TRONG callback (uncaught, KHÔNG fail pm.test) → hàm đệ quy `walk` chết giữa chừng → 3 assert cuối (trong callback done) không bao giờ chạy. Dấu hiệu CHÍ MẠNG: **count assertion dao động giữa các run** (PROD run2 48 vs run3 51, đều "0 failed"). 0-failed ru ngủ → tưởng pass. Verified 2026-06-17 get-invite-friends TC02 (84 GQL tuần tự, PROD) | (1) ĐỪNG dùng `HELPERS.gql` cho loop dài — viết `safeGql` TỰ `pm.sendRequest`, callback **KHÔNG BAO GIỜ ném**: `if(err||!res){cb(true,null);return;} try{ents=res.json()...}catch(e){cb(true,null);return;}`. (2) Phân biệt **err (mạng/timeout → BỎ QUA, đếm `skipped`, KHÔNG kết tội phần tử)** vs **empty (200 nhưng 0 ent → kết tội thật)** — nếu không, 1 timeout → "user không tồn tại" = false-FAIL. (3) Chống vacuous: thêm assert **"đối chiếu được ≥80% (checked/total)"** — mass-timeout thì FAIL honest thay vì pass-giả với 0 thực-kiểm. (4) Pre-flight: nếu count assertion đổi giữa các run = CHƯA xong, đào async đứt |
| B31 | Test API xóa/sửa mà bộ TC chỉ có case NEGATIVE (xóa cái không tồn tại / thiếu field) → **THIẾU HAPPY-PATH**: chưa hề chứng minh "xóa được thật + đối tượng biến mất". NLLB bắt: "muốn xóa related thì phải ADD related đã, rồi mới báo xóa thành công + xác nhận hết" | API delete/update viết test kiểu "gọi đại → FAILED/400" KHÔNG verify hành vi BUSINESS chính (xóa OK + DB hết bản ghi). Migration càng dễ sót vì bê nguyên TC cũ (cũng thiếu). Cần B-A-R-C: **dựng** đối tượng → **xóa** (assert status OK + message) → **đối chiếu DB** (GqlQuery count 0) → **gọi lại** (FAILED = xác nhận hết). Verified KF-28047 related-user/delete 2026-06-17 | Mọi API mutate: BẮT BUỘC ≥1 TC happy dựng-state-thật rồi thao tác rồi đối chiếu nguồn độc lập (GqlQuery/Firebase) đã đổi đúng. Negative-only = chưa test cái cần. Tìm seam dựng state: business-API nghịch, hoặc test-data API (`reset-test-data`), KHÔNG bịa data giả nếu có cơ chế thật |
| B32 | Định dùng `utility/web/admin/entity/edit` để TẠO entity test (RelatedUser, BannedObject…) → **404 "Không tìm thấy <kind> id=…"**: entity/edit CHỈ sửa entity ĐÃ TỒN TẠI, KHÔNG create. Và "device key" thấy trong `User.linkedUser` có thể là **synthetic key KHÔNG có Device entity** → mc auth 401 "Device not found" dù user có vẻ link. ⚠ ĐỪNG vớ `reset-test-data` để dựng 1 entity → SAI DOMAIN (nó reset CẢ pool test) | (1) `EntityController.editEntity` `datastore.get(key)` null → 404 (chỉ update field; backup "EDIT"). `entity/delete` DEV-only (403 prod)+backup; `entity/restore {backupId}` ghi lại entity về originalKey của backup — **dựng TARGETED 1 entity**. (2) Reset pool set `User.linkedUser = Key(Device,'RESET-RELATED-PAIR-<min>-<max>')` = key ẢO display, KHÔNG tạo Device row → `deviceSharedService.findById` null. mc resolve caller = Device THẬT qua `getUserByDeviceAndLanguage`: `en.yokara`→`linkedUserYokara`, `vi`→`linkedUser`. Verified KF-28047 2026-06-17/18 | Dựng 1 entity test (KHÔNG có business-API tạo): **`entity/restore {backupId}`** — bootstrap backup 1 lần (`entity/edit` entity sẵn có → tạo row `EntityBackup`, ko prune → id ổn định; query `GqlQuery EntityBackup WHERE originalKey=KEY(<kind>,'<id>')` lấy id), rồi [Setup] restore mỗi run (idempotent, isolated, ~200ms). **`reset-test-data` = API reset cả pool 19 TK → CHỈ bootstrap lần đầu, KHÔNG làm seed per-TC** (NLLB chốt 2026-06-18). Caller device THẬT: GqlQuery `Device WHERE linkedUserYokara=KEY(User,'<fb>')` + `language=en.yokara`. entity/edit/delete/restore luôn cần `x-admin-token: {{utility_admin_token}}` (thiếu → 401) |
| B33 | Build request qua PUT collection mà để `url` = `{"raw":"{{base_url}}/path"}` (CHỈ field raw) → Postman lưu thành `{"raw":""}` (RỖNG) → Newman chạy **MỌI request "request url is empty"**, `pm.response` undefined, toàn bộ assert đỏ `expected undefined to deeply equal 200` / `"undefined" is not valid JSON` (tưởng body/script sai, thực ra request KHÔNG chạy) | Postman REST khi PUT cả collection cần `url` dạng **STRING** hoặc **object đủ `host`+`path`** (`{"raw":"{{base_url}}/v39.X","host":["{{base_url}}"],"path":["v39.X"]}`). Chỉ `{raw}` → API nuốt thành rỗng. Verified delete-comment 2026-06-19 (54/54 url empty → thêm host/path → 0 lỗi) | Helper dựng url PHẢI build object có host+path (tách `?query` ra `query[]`): so sánh format với folder cũ chạy được (`getCollection` → url của request bất kỳ). SAU push, verify `stored url['raw']` không rỗng trước khi Newman |
| B34 | Đặt tên request kiểu `"TC01 [Action] ..."` (số TC trước tag) hoặc tạo sub-folder `[Teardown]` → **hook lint `lint_before_newman.sh` CHẶN newman** (R3 "request thiếu tag trạng thái", R2 "sub-folder sai tên") | §5 CHỐT: tên request PHẢI **bắt đầu bằng** đúng 1 tag `[Setup]/[Before]/[Action]/[Recheck]/[Cleanup]` (bracket khít, KHÔNG `[Before 0]`/`[Before Send]`); sub-folder CHỈ `[Setup]` hoặc `TC<NN> - mô tả` (KHÔNG `[Teardown]` — unset biến nhét vào... thực ra BỎ teardown, env-var để lại debug). responseTime chỉ trong request tên mở đầu `[Action]`. Verified delete-comment 2026-06-19 (57 vi phạm) | Build script: hàm normalize tên `re.sub(r'^TC\d+\s+','')` + gộp `[Tag ...]`→`[Tag]`; số TC nằm ở tên SUB-FOLDER, không lặp ở request. Bỏ folder [Teardown] |
| B35 | Test nhánh "từ chối quyền" (người-lạ KHÔNG được xoá/sửa) bằng fixture NLLB → server VẪN cho làm → assert đỏ HOẶC (tệ hơn) đổi assert thành "cho phép" = che mất lỗ hổng | MỌI fixture NLLB (owner 566 / member 438 / newUser 849 / guest…) đều `role=TESTER` (vài cái `SINGER`). `UserSharedUtils.isCustomerSupporter` trả TRUE cho `{CUSTOMER-SUPPORTER,MANAGER,ICOIN-MANAGER,TESTER,TESTER_AUTO,SINGER}` → mọi fixture = CSKH → **bypass mọi check quyền chủ-sở-hữu** (xoá/sửa comment, xem private…). test-state API (`/mc-user/test/v1/entity`) KHÔNG bật cho kind `User` (PUT role → 500) → KHÔNG flip role tạm được. Verified delete-comment 2026-06-19 | Nhánh từ-chối-quyền cần fixture **role thường (USER)** đã login — KHÔNG có sẵn ⇒ ghi GAP vào report + folder description, KHÔNG ép xanh/đảo assert. Vẫn test nhánh `isAdmin` (CSKH xoá được comment người khác) để chứng minh gate hoạt động. Recall [[role-tester-bypass-block]] |
| B36 | Request legacy (`v40.GqlQuery`, `v39/v40.LoginForTester`) trong folder NLLB trả 200 nhưng **0 entity / "parameters is null"** → tưởng query sai/data rỗng | Pre-request **ký `parameters`** chỉ nằm ở **folder AppApi** (folder-level), KHÔNG phải collection-level. Folder NLLB tạo Ở TOP-LEVEL (ngoài AppApi) → request legacy KHÔNG được ký → `/v40.GqlQuery` đọc `req.getParameter("parameters")`=null → `{"status":"FAILED","message":"Cannot read field GqlQuery because parameters is null"}` (374B, vẫn HTTP 200). mc-recording (path có `mobile`) KHÔNG cần ký nên chạy bình thường → chỉ GqlQuery/login đỏ. Verified comment-list 2026-06-20 (17 GAE-assert đỏ) | TỰ KÝ ở **request-level pre-request** cho CHỈ request legacy: resolve `{{var}}` trong body → `data=btoa(encodeURIComponent(body))` → `key = (digits-trong-base64 ĐẢO-NGƯỢC + "3564")[:15] as Number + 7123358024` → body=`parameters=<data>-<key>` + header `Content-Type: x-www-form-urlencoded`. mc-recording GIỮ raw JSON (đừng ký). GqlQuery KEY syntax: `KEY(Recording, <số>)` / `KEY(RecordingComment, '<id>')` (kind KHÔNG quote). Hoặc: đặt folder TRONG AppApi để kế thừa ký (nhưng nó sẽ ký luôn cả request mobile → hỏng) → KHÔNG nên; tự-ký-request-level an toàn hơn |
| B30 | Setup `.set()` / assert một biến mà **KHÔNG TC nào trong suite tiêu thụ** → script phình, lừa người đọc tưởng biến đó cần, re-push còn ghi đè tay người khác | Lười copy-paste request login/setup từ suite khác → kéo theo cả những dòng `pm.environment.set("...", ...)` + assert field mà suite HIỆN TẠI không dùng. Verified 2026-06-17 KF-27807 /groups/members: 4 login lưu `Nllb_group_firebaseToken_<role>` + assert `u.firebaseToken`, nhưng cả folder **0 consumer** (`{{...firebaseToken}}` 0 ref, không có `AddUserOnline`) — firebaseToken chỉ cần ở suite có `v11-AddUserOnline` (set user online phòng); /groups/members chỉ đọc list nên thừa hẳn. Console.log còn ghi "...cho AddUserOnline" = dấu vết copy-paste | **Set/assert biến CHỈ KHI có consumer thật trong CHÍNH suite này.** Trước khi `pm.environment.set(X)` hoặc assert field Y: grep suite có `{{X}}` / `.get("X")` / dùng Y ở đâu không — KHÔNG có → BỎ. Pre-flight build: mỗi biến set trong setup phải truy ra ≥1 nơi đọc (body/script TC) trong cùng folder; biến "để dành cho tương lai" = rác, đừng set. Copy request từ suite khác → cắt phần set/assert không liên quan API đang test. (Giữ cái CÓ consumer: vd `Nllb_group_password_*` dùng cho AES gate /v34 → giữ.) |
| B37 | Tự gõ lại **hằng số crypto** (private key / AES key) "từ trí nhớ" để probe/cleanup → lệch vài ký tự → password sai → server `FAILED "Wrong password"`, và NẾU đang dùng để cleanup-relink thì **device fixture bị bỏ link sai chỗ** (mutate state thật) mà ngỡ "đã restore" | Probe login mc-user ngoài Postman: tái hiện `AESDigitalSignature.encryption(uid)` = `key=sha1(b64decode(PRIV))[:16]` → `openssl enc -aes-128-ecb -K <hex> -nosalt`. PRIV gõ tay lệch `CCASwG/ggEf…` ↔ `CCASsG/ggEd…` → key SHA1 khác hẳn → pwd sai. Hàm probe in `pwd("122093251785185428")` ra `kE2hSn6…` thay vì known-good `Gu1b33aJFn/sPeOBVZMERaEA3esPgahRJl7ylSbWX3E=`. Verified 2026-06-21 logcat-replay: cleanup owner FAILED → device fixture còn dính apple user. | **KHÔNG hardcode key bằng trí nhớ** — copy NGUYÊN từ source build (`build_login.py:68`). Probe phải có **assert self-check** (vd in pwd của 1 uid known → so chuỗi known-good) TRƯỚC khi bắn cleanup. Cleanup-relink xong PHẢI verify lại `facebookId` trả về == owner; nếu ≠ → device chưa restore, sửa key rồi chạy lại. |
| B39 | Giữ `[Setup]` gọi 1 endpoint phụ (vd `v44.GetLastestVersion` "đảm bảo device tồn tại") để cho chắc → endpoint đó server lỗi/regress (500) → **cả suite đỏ vì 1 precondition THỪA**, không liên quan hành vi đang test | [Setup] GLV vốn để "ensure device" NHƯNG device fixture đã tồn tại sẵn — happy-path trả 200 + `data.user` ĐÃ chứng minh device thật (login fail trước ở device-check nếu thiếu). GLV = thừa. 2026-06-23 `v44.GetLastestVersion` trên DEV trả 500 deterministic (2 ngày trước còn 200) → 7 folder [Setup] đỏ. Giữ nó = vừa thừa vừa cột suite vào 1 endpoint ngoài scope | Precondition chỉ giữ khi suite THỰC SỰ cần nó để chạy — nếu happy-path đã ngầm chứng minh precondition thì BỎ [Setup] đó. KHÔNG mask 500-server bằng cách hạ assert [Setup] xuống lenient (xanh-giả). Endpoint phụ 500 ngoài scope → ghi finding cho dev + bỏ khỏi suite, đừng để nó quyết định pass/fail của API đang test |
| B40 | Request GqlQuery verify dựng body THỦ CÔNG ở **request-level pre-request** rồi `pm.variables.set('reqbody', 'parameters=...')` + body item = `{{reqbody}}` → Newman trả **0 entity (`status:FAILED`)** dù entity CÓ THẬT (curl tay cùng query ra 1) → recheck B-A-R đỏ toàn bộ, tưởng "async không ghi / write hỏng" | Collection `AutomationTest - Ngocllb` CÓ **pre-request cấp-collection tự-ký legacy** (`handleLegacyRequest`): chạy **TRƯỚC** request-level pre-request, đọc `pm.request.body.raw` = literal `{{reqbody}}` (template CHƯA resolve; `reqbody` lại là `pm.variables` nên `replaceEnvironmentVariables` bằng `pm.environment.get` KHÔNG thấy → giữ nguyên), thấy không bắt đầu `"parameters"` → **base64 CHÍNH chuỗi `{{reqbody}}`** + ký + `updateBody()` ghi đè. → server nhận GqlQuery rác → `{"status":"FAILED"}` → `gqlQueryEntities` undefined → 0 ent. Request-level set `reqbody` SAU đó vô dụng (body đã bị thay bằng chuỗi cố định, template mất). Đây là biến thể NGƯỢC của B36: ở collection NÀY legacy ĐƯỢC auto-ký sẵn, nên TỰ ký lại = phá. Verified 2026-06-23 yt-song/interactions TC01/02 recheck (10 assert đỏ; TC03 còn **false-pass** vì query rác→0 mà nó kỳ vọng 0). Write path & async HOÀN TOÀN khỏe (probe ký tay ghi 1s, addTime advance đúng) | **ĐỪNG tự dựng+ký body cho GqlQuery trong collection có auto-signer.** Để body item = **raw JSON THUẦN** `{"GqlQuery":"SELECT ... KEY(Kind,'{{env_var}}#...')"}` (chỉ dùng `{{env_var}}` env-level, KHÔNG `pm.variables`, KHÔNG hàm tính trong pre-request vì pre-request chạy SAU collection-signer) → collection `handleLegacyRequest` tự resolve env + base64 + ký. Xoá hẳn request-level pre-request ký. Nếu key cần phần tính → set env var đó ở **[Setup]/request SIBLING TRƯỚC** (kịp trước collection-signer của request sau). Phát hiện: count assert/giá trị baseline = "(chưa có bản ghi)" trong khi GqlQuery tay ra data → nghi body bị collection-signer mangle (gửi mangled body `parameters=<b64('{{reqbody}}')>-key` bằng curl, ra `status:FAILED` là chốt) |
| B40 | TC "negative" gửi id GIẢ (`NLLB-GHOST-…`) tới endpoint **tạo-khi-không-thấy** → vô tình TẠO THẬT entity đó → các TC sau dùng lại đúng id ấy thấy "đã tồn tại" → **false-pass / hành vi đổi**, mà tưởng test ổn định | mc-user login: apple-test (sai) `newUserApp("NLLB-GHOST-NOPE-999")` tạo user ghost trong dev datastore. Sau đó recent/related test user-not-found dùng LẠI cùng id `NLLB-GHOST-NOPE-999` + `AES(id)` (mật khẩu hợp lệ vì AES là hàm của id) → `findById` thấy ghost → login OK thay vì "không tồn tại" → đỏ. Tự mình làm bẩn env rồi tự mình false-pass. Verified 2026-06-23 | id "ma" cho test not-found PHẢI (1) **chưa từng được tạo** + (2) **KHÔNG bao giờ gửi tới method có auto-create** (apple). Dùng id riêng-mục-đích, đặc trưng (`NLLB-NOUSER-RECENT-…`), KHÁc nhau giữa các method. Sau khi lỡ tạo ghost qua endpoint không có API xoá → ghi nhận orphan vào report (không xoá được = chấp nhận), nhưng đổi id để test không dính. (gắn A24 auto-create + verify-this-run) |
| B38 | Sinh `pm.test(name, () => { ...nhiều câu... })` thân arrow DÀI gộp nhiều `pm.expect`+`console.log` → Newman/uvm ném `SyntaxError: missing ) after argument list` ⇒ **cả test-script chết, assertion KHÔNG đếm** (Newman vẫn "assertions X/X 0-failed" vì khối hỏng không chạy assert nào) | uvm (vm2 sandbox của Newman) parse strict hơn `node`; thân arrow rất dài (nhiều statement + chuỗi `+` nối console.log + tên test chứa `()`/`+`) đôi khi vấp "missing )" dù `node --check` từng-phần qua. Verified 2026-06-21 login TC04 facebook: 1 pm.test gộp 2 expect + console.log dài 480 ký tự → đỏ; test-scripts `28/1` nhưng assertions hiển thị đủ (đánh lừa). | Tách MỖI assertion thành 1 `pm.test` ngắn (1 expect/test), tên test KHÔNG nhồi `+`/ngoặc kép-lồng. **`node --check` MỌI script sinh ra TRƯỚC push** (loop qua `event.script.exec`, ghép `\n`, check); và đọc dòng `test-scripts │ executed │ failed` của Newman — `failed>0` = có script chết, KHÔNG được tin con số assertions. |
| B41 | TC tra `postId`/id của bài-vừa-tạo bằng `pm.sendRequest` (gọi feed mc trong pre-request) → trên **PROD trả 400** (`INVALID_SIGNATURE`/`UNAUTHORIZED`) → `found=false`, id=undefined → đổ dây chuyền: action chính `VALIDATION_FAILED`, recheck sai, cleanup `delete` body `"postId": {{var}}` (var rỗng → JSON `{{...}}` không quote = **"Malformed request body"**) retry 60 lần. **DEV xanh / PROD đỏ** → nhìn như flaky/"chạy không ổn định". | `pm.sendRequest` **KHÔNG chạy pre-request cấp-collection** (`handleMicroServiceRequest` upsert header `X-Signature`) → request thủ công bay đi **thiếu chữ ký**. **DEV không enforce signature** nên sendRequest vẫn chạy → từng pass DEV; **PROD enforce** → 400. Đây là biến thể signature của [[feedback_cleanup_sibling]]/[[feedback_poll_async_score]] (sendRequest bypass collection script). Verified 2026-06-24 KF-27809 TC06 (4 fail PROD, lookup `/groups/posts` trong `[Action]` prerequest). | **ĐỪNG dựng request mc bằng tay (`sendRequest`/tự ký) trong test.** Tra id = tách thành **1 request CHÍNH sibling** (POST feed, type=ALL) đặt TRƯỚC action → pre-script collection tự ký như mọi request → test-script của nó `find(p=>p.postMessage===msg)` rồi `pm.environment.set(idVar, String(found.postId))`. Action sau chỉ dùng `{{idVar}}`. (TC04/TC05 cùng KF làm vậy → pass cả 2 env; TC06 trước đó dùng sendRequest → chỉ đỏ PROD). **Đọc pre-script cấp-collection TRƯỚC khi sửa/dựng request mc** — signature/encode đã lo sẵn ở đó, tự khai báo lại = lệch. KHÔNG đụng pre-script chung (dùng chung nhiều folder/tester). |
| B42 | Self-loop poll-retry viết `else { postman.setNextRequest(null); ...assert }` tưởng `null` = "thôi loop, chạy tiếp request kế" → thực ra **DỪNG TOÀN BỘ run** → mọi TC sau biến mất (vd send-logcat: chỉ TC01 chạy, 3/17 request, summary `requests: 3` nhưng không báo lỗi → dễ tưởng "chỉ TC01 có" rồi push thiếu) | `postman.setNextRequest(null)` trong Postman/Newman = **kết thúc run NGAY** sau request hiện tại, KHÔNG phải "tiếp tục tuần tự". `setNextRequest` chỉ nên gọi khi MUỐN nhảy (retry self / chỉ định request kế); muốn chạy linear bình thường thì **KHÔNG gọi** | Self-loop: CHỈ `setNextRequest(pm.info.requestName)` ở nhánh **retry**; nhánh **done để TRỐNG** (không gọi gì) → Newman tự sang request kế (giá trị self ở vòng trước KHÔNG carry-over, mỗi lần chạy script reset về linear). Dấu hiệu dính lỗi: tổng `requests` trong summary < số request thật của folder. Verified send-logcat KF-28453 2026-06-24 |

---

## C. MÔI TRƯỜNG & ĐỒNG BỘ NHIỀU MÁY – NHIỀU NGƯỜI ⭐ (lý do chính cần file dùng chung)

| # | 🟢 Nói dễ hiểu (non-tech) | 🔧 Bản kỹ thuật (tech) | Cách tránh |
|---|---|---|---|
| C1 | Mỗi máy đang chạy một bản test khác nhau | Bản local stale ≠ bản người khác sửa tay trên collection/Zephyr → cùng test khác kết quả | Trước khi chạy LẤY BẢN MỚI NHẤT từ nguồn chung (refetch collection+env, `rm /tmp/coll_*.json`); đừng tin bản đang lưu |
| C2 | Dùng chung tài khoản/biến rồi giẫm chân nhau | Env DEV/PROD shared nhiều tester (`linhltt_/nnn_/anhtd_`); đụng biến/fixture người khác → state đổi giữa chừng | Chỉ dùng prefix/tài nguyên được phân (vd `NLLB_`); không đụng của tester khác |
| C3 | Đẩy kết quả vào nhầm chỗ chung của người khác | Push nhầm collection (GAE shared `8e31842d…` thay vì `AutomationTest - Ngocllb`) → đè việc người khác | Xác nhận đúng COL_ID + đúng folderId trước khi PUT/push |
| C4 | Tin dữ liệu cũ trên máy thay vì dữ liệu thật hiện tại | Cache `/tmp/coll_*.json` cũ → báo cáo sai (vd báo 55 fail trong khi thật chỉ 1) | Xoá cache + refetch collection/env trước MỖI lần chạy Newman |
| C5 | Chạy máy/môi trường này ổn nhưng nơi khác hỏng | Dev pass nhưng prod fail do thiếu composite `index.yaml` / config drift dev↔prod | Verify cả dev & prod 3-run; coi prod là chuẩn cuối; lệch cấu hình → ghi rõ report |
| C6 | Giả định công cụ/khoá trên máy mình ai cũng có | Hardcode dựa API key / tool có ở máy mình; máy khác chạy thiếu → lỗi | Ghi rõ thứ cần chuẩn bị (key, tool); đừng giả định môi trường đồng nhất |
| C7 | Push qua API thành công nhưng lát sau bản cũ tự quay lại, người kia "chưa thấy gì" | App Postman đang MỞ collection với bản cũ → auto-sync đẩy bản local (cũ) **đè ngược** lên bản vừa PUT (lost-update từ phía app, KHÁC B16 là window fetch↔PUT của chính mình). Dấu hiệu: PUT OK + fetch-ngay-sau đúng, nhưng fetch lại sau vài phút ra bản cũ | Sau khi PUT: **fetch lại xác nhận**; nếu bị revert → người đang mở app phải **Pull/refresh (hoặc đóng) collection TRƯỚC** rồi mới push lại; đừng giữ tab có chỉnh sửa chưa lưu lúc đang push. Push↔mở-app đồng thời = đè qua đè lại |
| C8 | Refetch collection bị lỗi (API trả error JSON) → Newman không thấy folder → chạy 0 testcase → báo "0 fail" → tưởng PASS (xanh giả) | GET `/collections/<id>` đôi khi trả `{"collection":{"error":{"name":"instanceNotFoundError"}}}` (hiccup API/sai id-prefix) chỉ ~127 byte; ghi đè file collection. Newman → `Unable to find a folder or request: "<folder>"`, **0 assertion chạy** → `grep -c AssertionError = 0` → đếm nhầm là "0 lỗi = pass". Thực ra KHÔNG có gì chạy | Sau refetch: **verify file hợp lệ** (`'collection' in d and 'item' in d['collection']` + tìm thấy folder đích) TRƯỚC khi chạy. Sau khi chạy: **assert số request executed == số mong đợi** (vd 14), KHÔNG chỉ xem failed==0. Dùng `--reporters json` đọc `run.stats.requests.total` + `run.failures`; "0 fail" vô nghĩa nếu `requests.total==0`. (gắn E5: đọc đủ summary, đừng tin 1 con số). ⚠ Cùng triệu chứng còn do **cwd reset giữa mỗi lệnh Bash + thư mục tạm (`.nm_tmp`) bị dọn** → file `coll.json`/`env.json` (ghi bằng path tương đối) biến mất ở lệnh sau → newman `ENOENT could not load` → 0 assertion → "clean" giả. Dùng **path tuyệt đối + thư mục ổn định** (`/tmp/nllb/...`), và xác nhận `wc -c` file > vài KB trước khi chạy newman |
| C9 | Build test xong để lại cả mớ biến rác trên tài khoản chung, người khác nhìn vào không biết cái nào xoá được | Dùng `pm.environment.set()` cho giá trị SINH-RUNTIME (cursor, captured id, `_t0`/`_polls` counter, date tính-toán) → ghi thẳng vào env DEV/PROD shared → tích lũy (đã đếm 35 DEV + 21 PROD). Trộn lẫn với fixture thật → user không dám xoá (sợ trúng biến đang dùng). Build script còn tự đẻ biến **âm thầm**, không công khai trước → user phải soi ngược script | (1) **Runtime → `pm.variables.set()`** (local scope, Postman/Newman tự xoá khi run xong), KHÔNG `pm.environment.set`. (2) **Variable Manifest TRƯỚC build**: liệt kê biến `Nllb_*` đã seed sẵn → ƯU TIÊN TÁI DÙNG; trình bảng `Biến\|Loại\|Nguồn\|Mới/Tái-dùng` cho user soi trước khi build. (3) Mint env var mới = hạ sách cuối (cross-env + user phải seed) → LIST cho user, KHÔNG tự PUT. (4) **Guard tự động** `postman_helper.set_env_vars()` RAISE khi key mới khớp pattern runtime + in `[env MANIFEST]`. Chi tiết CLAUDE_NLLB §4.7. ⚠ **Dùng `pm.variables` ĐÚNG (0 residue) VẪN PHẢI trình Variable Manifest TRƯỚC build** — manifest để CÔNG KHAI + ƯU TIÊN TÁI DÙNG biến đã seed, KHÔNG chỉ để chống rác. Đừng rationalize "đã dùng pm.variables nên khỏi trình". Burn send-logcat KF-28453 2026-06-24: build thẳng bỏ manifest, user bắt "không đọc hết rule" |

---

## D. DỮ LIỆU & TRẠNG THÁI CHẬP CHỜN (test lúc đậu lúc rớt)

| # | 🟢 Nói dễ hiểu (non-tech) | 🔧 Bản kỹ thuật (tech) | Cách tránh |
|---|---|---|---|
| D1 | Xanh giả: test báo đậu nhưng không kiểm đúng cái cần | "Any entity matches filter" khớp nhầm entity CŨ → pass-giả lọt bug thật | Khoá id CỦA LẦN CHẠY NÀY (response id, hoặc `_t0=Date.now()-5000` filter `dateTime>t0`); unset stale env trước Send |
| D2 | Chạy 1 lần thấy xanh đã tưởng chắc | "1 lần pass" ≠ deterministic; flaky lần sau rớt | Verify lặp 3-run/env; xanh ổn định mới tính done |
| D3 | Không dọn dữ liệu nên lần sau bị nhiễu | Thiếu teardown → entity tích tụ qua các run, đếm sai | Mỗi test [Cleanup] bằng API delete chính thống cùng domain (KHÔNG xoá thẳng Firebase / fake flag) |
| D4 | Đếm/so ngay sau thao tác cần thời gian xử lý | So sánh ngay sau async commit (send-gift contrib lag ~2.3s) → số chưa cập nhật | Poll-based recheck (`pm.sendRequest`+setTimeout) hoặc self-loop `setNextRequest` tới khi settle |
| D5 | Run trước bị NGẮT giữa chừng để lại "lời mời/yêu cầu" sót → run sau rớt 1 case lạ | State sót (vd Firebase `roomUserScore/<room>/invites/<fb>` chưa clear) làm assertion recheck-NEGATIVE ("đã xoá") đỏ; thao tác chính (re-invite) VẪN success nên KHÔNG phải bug API | Khi 1 case fail sau khi đã pass: PROBE state thực tế (GQL+Firebase) TRƯỚC khi đào code; dọn leftover bằng API nghịch chính thống (DECLINE/cancel) rồi chạy lại; cân nhắc pre-clean ở [Setup] cho suite có invite/request |
| D7 | Gắn "thử lại tự động" cho nhiều request TRÙNG TÊN → nó nhảy nhầm về cái đầu, lặp loạn | `postman.setNextRequest(pm.info.requestName)` (self-loop retry kiểu D4) dựa vào TÊN; trong 1 folder có ≥2 request trùng tên (vd 5 recheck "[Recheck] all-group") thì Newman/Postman luôn nhảy về request ĐẦU TIÊN khớp tên → retry lặp sai chỗ, đếm/state loạn, có khi treo vòng | Trước khi gắn self-loop: ĐẢM BẢO mỗi request đặt **tên DUY NHẤT** trong folder (gắn hậu tố phân biệt: "(member STATUS)"/"(owner mode ALL)"…). Quy tắc chung: bất kỳ cơ chế điều hướng theo tên (`setNextRequest`) đều yêu cầu tên unique |
| D8 | TC "verify không đổi" (negative) rớt chập chờn vì THAO TÁC THÀNH CÔNG của TC TRƯỚC chưa kịp ghi xong (cache lag) lúc TC này chụp baseline | API microservice ghi xong (`update-properties` 200) nhưng read-endpoint (`groups/detail`) cache eventual-consistency → giá trị cập nhật landed trễ. TC sau gọi `[Before]` chụp baseline lúc write của TC trước CÒN settling → baseline lệch giá trị recheck đọc sau đó → assert "không đổi" đỏ (dù TC này 4 request đều 401/từ chối, KHÔNG hề đổi gì). Fixed-delay càng nhỏ càng dễ dính, DEV/PROD đều có | (1) MỌI recheck đọc-lại giá-trị-vừa-ghi → **poll-retry self-loop** (`setNextRequest`, MAX ~15, early-exit) thay fixed-delay. (2) TC có [Restore] → thêm **settle-poll** cuối TC (chờ giá trị revert đúng baseline) để TC kế khởi động trên state đã settled. (3) TC negative chụp baseline [Before] rồi so chính baseline đó — vẫn cần state vào TC đã ổn định. Burn KF-27805 2026-06-15 (TC05 reject-recheck bị nhiễm bởi restore TC04) |
| D9 | Counter "lượt xem/like" trả về nhưng 2 lần probe ra `+1` đều đặn → tưởng chắc, viết assert `===+1`, lần 3 ra `+2` → xanh-giả-rồi-flaky | Counter API kiểu buff-ảo (`increase-simple-view-counter`) trả **giá trị lạc quan** (cache-miss=`stored+1` CHƯA ghi), persist thật qua **Cloud Task buff async** lag sub-giây→vài giây KHÔNG đều. Delta giữa 2 call back-to-back phụ thuộc buff có commit chen vào hay không → non-deterministic. 2 probe đầu trùng `+1` chỉ là buff-trễ may mắn; probe 3 buff commit giữa chừng → `+2`. Lock `===vMiss-1`/`===vTester+1` từ 2 mẫu = bẫy false-green (verified 2026-06-18 increase-simple-view-counter) | (1) Counter không `save()` đồng bộ (chỉ `++` rồi `return`, có `cloudTask.post(buff)`) → ĐỪNG assert delta cứng. (2) Bất biến robust DUY NHẤT = `stored` **đơn điệu tăng** → so `>`/`>=`: "guest-view > mốc-do-account-không-đếm(TESTER)-đọc-trước" luôn đúng (S'≥S), chứng minh cả tăng-và-không-tăng mà không false-red. (3) Probe ≥3 lần ở thời điểm KHÁC nhau trước khi tin 1 delta — 2 mẫu trùng chưa đủ. (4) Hiệu ứng dedup/idempotent lên counter buff-async thường KHÔNG observable → ghi blocker, đừng ép assert |
| D6 | Chủ nhóm fixture "đúng là chủ" nhưng PROD lại không cho thao tác → tưởng bug code, thực ra DỮ LIỆU group thiếu dòng chủ nhóm trong bảng thành viên | Permission đọc từ membership table (`LiveRoomUser` role) chứ KHÔNG fallback `LiveRoom.ownerKey`. PROD có group mà `LiveRoom.ownerKey`=owner NHƯNG `LiveRoomUser` 0 row role=OWNER (owner thiếu hẳn khỏi member list) → owner-self-action (vd posts/delete) bị từ chối `FAILED "Không thể xoá bài đăng!"`. DEV data lành → pass. Message KHÁC nhau ("không thể xoá" vs "không phải thành viên") tuỳ version deploy lệch source → đừng over-claim đúng dòng code | Owner-action fail trên 1 env: GQL probe `LiveRoomUser WHERE liveRoomKey=KEY(...)` đối chiếu `LiveRoom.ownerKey` + `Device.linkedUser*` TRƯỚC khi kết luận. Lệch data = bug DATA (seed/migrate), không phải test sai; report đủ 3 fact (entity ownerKey / membership rows / post facebookOwnerKey). Fixture chủ nhóm phải có row OWNER trong CHÍNH group test |
| D11 | Được báo "dev chưa trả field X do bug" → định viết assert fail-sẵn — nhưng thực ra field RỖNG vì BÀI GỐC vốn không có data X, không phải code bug | Verify field hiển thị (vd `mixedRecordingVideoUrl` cho SHARE_VIDEO): fixture video CŨ là FB-reel có `mixedVideoUrl`=NULL ngay trong DB (url ở `subVideos`) → response thiếu field. Dán nhãn "[Chờ dev fix] bug mapper" là SAI: đổi fixture sang bài VIDEO thật có `mixedVideoUrl` populated → DEV TRẢ ĐÚNG field. (Bonus: source local PostMapper gate `postType null/RECORDING` lẽ ra skip, NHƯNG bản deploy mới hơn source snapshot đã trả → đừng kết luận chỉ từ source) | Trước khi dán nhãn "dev chưa trả/bug": (1) PROBE bài gốc trong DB (GqlQuery) xem field X có data THẬT không — null nguồn ≠ bug code; (2) chọn fixture có data đầy đủ rồi DUMP response thật (`-r json`) xem server trả gì; (3) chỉ khi data nguồn CÓ mà response THIẾU mới là bug. Verify LIVE thắng cả source-read (deploy có thể mới hơn). **(4) Assert field hiển thị KHÔNG dùng `.not.empty`/`above(0)` (chỉ chứng minh "có gì đó") — phải probe ground-truth từ bảng nguồn rồi `.to.eql(giá-trị-thật)` + regex định dạng (https + đuôi .png/.mp4) để chứng minh trả ĐÚNG data, không null/lỗi/giá-trị-rác.** Burn KF-27998 2026-06-19 |
| D10 | Dev báo "đã fix" → verify đọc page 1 thấy KẾT QUẢ CŨ (vẫn còn account/giá trị lẽ ra đã bị lọc) → vội kết luận "fix chưa ăn" — nhưng có thể chỉ là **cache server cũ** che, hoặc NGƯỢC LẠI "đã ăn" giả vì page1 vừa được cache lại | `top-users` cache Redis `TOP_USERS_TTL=8h` theo key `type:language:cursor` (`v33.TopUsers:2:en.yokara.fbuser:first`). Đọc lại page1 = có thể trúng snapshot cũ tính bởi code TRƯỚC fix → leak account nội bộ "ma". Tin 1 lần đọc page1 → kết luận sai cả 2 chiều (fix ăn/chưa). Phải tách **cache-stale** vs **deploy thật** | Verify 1 fix trên endpoint có server-cache TTL dài: **cache-bust để ép compute TƯƠI bằng code đang deploy** — (a) đọc **trang 2** (cursor → cacheKey ≠ `first`, gần như chắc miss → tính mới); (b) đổi 1 chiều key (language) lấy partition chưa cache; rồi đối chiếu. Chỉ kết luận khi **fresh-compute path** xác nhận. (verified 2026-06-19 top-users: page2 tươi VẪN leak `TESTER` → fix THỰC SỰ chưa hiệu lực, KHÔNG phải cache. Lưu ý partition khác data: `language=vi` ra 0 nội bộ KHÔNG chứng minh filter — chỉ là vi tự sạch). Gắn bất biến cache CLAUDE.md (`save()` không invalidate Redis) + B12 read-after-write |
| D12 | Được báo "TCxx **LUÔN** fail ở step đọc-lại-điểm (sau)" → định chộp lý do đầu tiên: body gift hardcode `requestTime` quá khứ (2026-05-22) → "ghi sai mốc tuần" → sửa requestTime. **SAI 2 lớp**: (1) chạy thử KHÔNG tái hiện — pass 6× cả DEV+PROD (cô lập + full-folder + 3× liên tiếp); (2) đọc 2 phía: `mobile/v1/SendGiftController` **GHI ĐÈ** `requestTime = System.currentTimeMillis()` ("client có thể bị trick") TRƯỚC khi đẩy task → requestTime body là **red herring**, write luôn vào **tuần hiện tại** | KF-27808 top-group-members TC05 cộng điểm Cống hiến/Sôi nổi: write async qua cloud task `/tasks/groups/add-score-group-user`, `score=noItem*buyPrice*10`, ghi TopUser key `weekBucket#TYPE#fbid#gid` cho sender(contrib)+receiver(engage) NẾU là member. Test exact-eq `after===truoc+30`, robust-MAX before-read (né eventual-consistency "0 giả"). Bản Ngocllb hiện PASS deterministic (verified 2026-06-23 DEV 3× + PROD 3×, delta đúng +30, async land <2s). "Luôn fail" KHÔNG tái hiện trên collection này | **User nói "luôn fail" → TÁI HIỆN trên ĐÚNG env/collection TRƯỚC, đừng tin & sửa mù.** Pass → nói thẳng "không tái hiện" + bằng chứng N-run rồi truy fragility cấu trúc. Fragility = **exact-equality trên counter SHARED + ASYNC theo tuần**: (a) ngân sách poll (18s) PHẢI ≥ độ trễ tệ-nhất tài-liệu-hoá của endpoint (PROD BXH 17–41s) — đã nâng MAX_TRIES 60→150 (~45s, early-exit nên run khỏe vẫn nhanh, GIỮ exact-eq → không che undercount); (b) write đồng-tuần từ tester khác / hoạt động thật chen vào → delta ≠ +30 → fail THẬT (đúng, đừng nới `>=` che). Trace 2 phía (controller có override field client?) trước khi đổ cho input. Gắn [[feedback_verify_this_run]] + D9 counter async |

---

## E. GIAO TIẾP, PHẠM VI & BÁO CÁO (cho người sau hiểu đúng)

| # | 🟢 Nói dễ hiểu (non-tech) | 🔧 Bản kỹ thuật (tech) | Cách tránh |
|---|---|---|---|
| E1 | Báo "xong" trong khi còn bước chưa làm/chưa kiểm | Declare done thiếu sub-step / chưa verify-this-run | Chỉ sign-off khi đã verify đủ; còn vướng → nói rõ pending cái gì |
| E2 | Viết báo cáo/đặt tên bằng thuật ngữ khó, BA không hiểu | Tên TC/log toàn thuật ngữ (R⊆S, cursor, phantom) → BA không nắm | Viết ngôn ngữ thường có dấu; cần thì kèm bản kỹ thuật riêng (đúng tinh thần file này) |
| E3 | Che giấu/nói giảm khi test rớt hoặc bỏ qua bước | Report đẹp che fail/skip → quyết định dựa thông tin sai | Báo trung thực: rớt thì nói rớt kèm output; bỏ bước thì nói đã bỏ |
| E4 | Tự quyết thay đổi lớn mà không nêu lý do | Silent decision không trace được, người sau không phản biện được | Nêu quyết định + lý do ngắn để người khác kiểm/phản biện |
| E5 | Đọc log test bằng "lấy N dòng cuối" → cắt mất bảng tổng kết → báo nhầm số fail | `tail -N` / cắt output Newman làm mất bảng `failures` + summary table (failures liệt kê SAU phần chi tiết) → đếm thiếu/sai số case rớt rồi báo cáo sai | Capture TRỌN output ra file (`> /tmp/run.txt 2>&1`) rồi đọc bảng summary + danh sách `# failure` đầy đủ; muốn lọc thì `grep -E "assertions|failures|AssertionError"`, KHÔNG `tail` cụt |

---

## F. CHECKLIST TRƯỚC KHI BẮT ĐẦU & SAU KHI XONG

**Trước khi bắt đầu:**
1. ✅ Đọc file này + `CLAUDE.md` + `CLAUDE_NLLB.md`; xác nhận đúng mục tiêu/đầu ra + đúng API/version. *(chống A1, A2)*
2. ✅ **Lấy bản mới nhất từ nguồn chung** (refetch + xoá cache), đừng tin bản cũ trên máy. *(chống C1, C4)*
3. ✅ Reuse tài nguyên/biến được phân (`Nllb_`/`NLLB_`); không tạo mới, không đụng của người khác. *(chống A9, C2)*
4. ✅ Làm từng bước, dừng confirm; không nhảy việc; không vượt phạm vi. *(chống A4, A5, A6)*
5. ✅ Probe response THẬT trước khi viết assert (shape, message, mã lỗi, nhánh thiếu field). *(chống B5, B6, B7)*

**Sau khi xong:**
6. ✅ Chạy lặp 3-run ở CẢ dev & prod → ổn định mới tính done. *(chống C5, D2)*
7. ✅ Đảm bảo ký + strip `OriginalRequest` + null-safe + delay chống throttle. *(chống B1, B3, B4)*
8. ✅ Dọn sạch data test đã tạo bằng API delete chính thống. *(chống B15, D3)*
9. ✅ Chưa chắc lỗi của ai → probe kiểm chứng trước khi gán cho dev. *(chống A7)*
10. ✅ Báo cáo trung thực, ngôn ngữ thường (kèm bản kỹ thuật nếu cần). *(chống E1, E2, E3)*
11. ✅ **Chạy linter cơ học** `python3 docs_migrate/postman/lint_tc_structure.py --file <coll.json> --folder "<KF>"` → PASS mới push/Newman (TC đánh số + request đủ tag 5-trạng-thái + tag tester + no-range + responseTime đúng [Action]). PreToolUse hook `lint_before_newman.sh` tự chặn `newman run` nếu sai. *(chống A19, A15 — gate máy thay audit thủ công hay lọt)*

---

## G. CÁCH CẬP NHẬT DANH SÁCH (cuối mỗi session/task — BẮT BUỘC)

1. Nhìn lại mọi lỗi/trục trặc ĐÃ GẶP trong phiên — đủ mọi nguồn (hiểu sai · kỹ thuật · môi trường-đồng bộ · dữ liệu · giao tiếp).
2. Đối chiếu A→E. Lỗi nào CHƯA có → thêm dòng mới vào **ĐÚNG nhóm theo nguồn gốc**, đánh số tiếp (A11, B16, C7…).
3. Mỗi lỗi mới viết **cả 2 tầng**: 🟢 nói dễ hiểu (non-tech) + 🔧 bản kỹ thuật — để báo cáo được cho người không rõ lẫn người rõ.
4. Lỗi đã có nhưng tái phát → KHÔNG nhân bản; chỉ bổ sung dấu hiệu/cách tránh cho rõ hơn.
5. Mục tiêu: lần sau máy nào người nào đọc cũng né được — **đồng bộ, nhất quán, đỡ chạy lại.**
